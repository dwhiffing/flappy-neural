export let FLAPPY_CONFIG = {
  gravity: 800,
  jumpHeight: -500,
  pipeSpeed: 200,
  playerCount: 100,
  pipeDelay: 3000,
  pipeDistance: 150,
  mutationRate: 0.1,
}

export let BREAKOUT_CONFIG = {
  playerCount: 1,
  playerSpeed: 500,
  bounceRatio: 0.5,
}
